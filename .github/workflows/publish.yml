name: Publish to npm

on:
  release:
    types: [published]

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    uses: yohn-jp/.github/.github/workflows/npm-publish.yml@4261a3fa11c084e0be3222219cc7429299399bed
    with:
      working-directory: .
      node-version: "22"
      smoke-test-node-versions: "22,24"
      npm-cli-version: "11.5.1"
