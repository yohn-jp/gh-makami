name: Publish to npm

on:
  release:
    types: [published]

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    uses: yohn-jp/.github/.github/workflows/npm-publish.yml@34846fadafb3f719b00ec7da6d78bcbcc4cfdc40
    with:
      working-directory: .
      node-version: "22"
      smoke-test-node-versions: "22,24"
      npm-cli-version: "11.5.1"
