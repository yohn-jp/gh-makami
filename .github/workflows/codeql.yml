name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "31 3 * * 1"

permissions:
  contents: read

jobs:
  analyze:
    permissions:
      contents: read
      security-events: write
      actions: read
    uses: yohn-jp/.github/.github/workflows/codeql.yml@f9ac0b0ea6f50b815d2f2b6f4c97650d79b9b994
    with:
      languages: "javascript-typescript,actions"
      config-file: .github/codeql/codeql-config.yml
