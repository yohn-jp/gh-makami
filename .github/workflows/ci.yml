name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  ci:
    uses: yohn-jp/.github/.github/workflows/typescript-cli-ci.yml@4261a3fa11c084e0be3222219cc7429299399bed
    with:
      working-directory: .
      node-version: "22"
